// write the cpp program to implement kth minimum and maximum element from unsorted array
#include <iostream.h>
using namespace std;
int main()
{
    int num,i,a[100];
    cout<<"enter the elements";
    cin>>num;
    for(i=0;i<num;i++)
    {
        cin>>a[i];
    }
    
    return 0;
}