#include <iostream>
using namespace std;
class Node
{
public:
    int data;
    Node *next;
};

int main()
{
   Node head = new Node();
   Node temp = new Node();
   if(head==NULL)
   {
       cout<<"we will not assign it";
   }
   else
   {
       for(int i=0;i<n;i++)
       {
           cin>>data;

       }
   }
    return 0;
}
